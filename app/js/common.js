$(document).ready(function() {
	$('.slider-sl').slick({
	  		autoplay:true,
		    autoplaySpeed:3000,
		    dots:true,
		    prevArrow:'.slider-controllers__prev',
        	nextArrow:'.slider-controllers__next',
	});
});